def f(i, k, s, t):  # i 원소 k지합의 크기 s i-1까지 고려된 합 t목표
    global cnt
    fcnt += 1
    if s > t:
        return
    elif s == t:
        cnt +=1


    if i == k:  # 모든원소 고려 합이 찾는 합보다 ㅡㄴㄱ
        if s == cnt
    a = [i for i in range(1,+4)]

# 작업하고자 하는 것에 따라